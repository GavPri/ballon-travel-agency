import React from "react";

const Logo = ({ width, height, classname }) => {
  return (
    <img
      width={width}
      height={height}
      src="/logoballon.png"
      alt="Logo"
    />
  );
};

export default Logo;
