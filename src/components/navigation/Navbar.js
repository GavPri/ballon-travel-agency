import React from "react";
import Logo from "../Logo";

const Navbar = () => {
  return (
    <nav className="max-w-[1240px] bg-dark h-24 m-auto">
      {/* ----- Logo */}
      <Logo width={100} height={100} />
      {/* ----- Nav links / unordered list */}
      {/* ----- Social media links */}
    </nav>
  );
};

export default Navbar;
